services:
  web:
    build: .
    container_name: healthcure-web
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: healthcure-docker-secret
      DATABASE_URL: sqlite:///healthcure.db
    volumes:
      - .:/app